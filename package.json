{
  "name": "simulacrum-space",
  "engines": {"npm":  ">=7.14.0"},
  "scripts": {
    "deploy": "git push https://${GITHUB}@github.com/krummja/Simulacrum-Heroku.git",
    "postinstall": "cd frontend && npm install && npm run build"
  }
}